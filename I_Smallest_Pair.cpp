#include <bits/stdc++.h>
using namespace std;

int main()
{
    int n;
    cin>>n;
    int a[n];
    int min = INT_MAX;
    for(int i=0; i<n; i++){
      for(int j=i+1; j<n; j++){
        if(a[i] + a[j] +j -i < min){
          min =
        }
      }
    }
    cout << min;

       return 0;
}