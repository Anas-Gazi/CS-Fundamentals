#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    string s;

    cin >> s;
    cout << s.substr(1, 5) << endl;
    cout << s.substr(2) << endl;

     return 0;
}