#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int t;
    cin >> t;
    while(t--){
      map<int, int> mp;
      int n;
      cin >> n;
      int arr;
      for(int i=0; i<n; i++){
        cin >> arr;
      }
int freq =0;
      for(auto x: mp){
        cout << mp.first()
      }
      
    }
    
     return 0;
}