#include <bits/stdc++.h>
using namespace std;

class Node{
  public:
     int data;
     Node* next; // store the address of the next Node.

     Node(int val){
      data = val;
      next = NULL;
     }
};

int main()
{
    int n;
    cin>>n;
    cout<<n;

       return 0;
}