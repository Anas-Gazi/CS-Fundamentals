#include<stdio.h>
int main(){
    int n;
    scanf("%d",&n);
    int x= n+5;
    printf("%d",x);
}
//This code takes an integer input from the user, adds 5 to it, and then prints the result.
//For example, if the user inputs 10, the output will be 15.